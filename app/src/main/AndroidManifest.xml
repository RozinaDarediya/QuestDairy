<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.admin.questdairy">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Spash_screen" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Register_screen" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".UserHomePage"
            android:label="@string/title_activity_user_home_page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Expert_Registration_Part2" />
        <activity android:name=".ExpertHomePage" />
        <activity android:name=".ExpertRegistrationRemaining" />
        <activity android:name=".Exception_Display_act" />
        <activity android:name=".Expert_profile" />
        <activity android:name=".ConnectivityChecking.NoInternetScreen"></activity>
    </application>

</manifest>